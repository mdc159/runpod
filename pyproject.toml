[project]
name = "runpod"
version = "0.1.0"
description = "RunPod management and automation toolkit for cloud GPU instances"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.28.0",
    "python-dotenv>=1.0.0",
    "boto3>=1.26.0",
    "paramiko>=2.11.0",
]
